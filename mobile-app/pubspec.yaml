name: school_support_mobile
description: Mobile application for the school support portal.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0
  flutter_secure_storage: ^10.3.1
  local_auth: ^3.0.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/altair-app-icon.png
