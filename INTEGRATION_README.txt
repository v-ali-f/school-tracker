ШКОЛЬНЫЙ ПОРТАЛ (school-tracker) — сборка от 01.06.2026
=========================================================

ЧТО ЭТО
Ваш актуальный main (включая desktop-app/ — Electron-обёртку) + слой UI/UX-полировки,
наложенный поверх. Слито без потери вашего функционала: где вы недавно сами переписывали
страницы (ознакомления, реестр приказов) — оставлены ВАШИ версии; полировка применена к
остальным ~140 шаблонам и глобально в app.css.

ЧТО В ПОЛИРОВКЕ (кратко)
- Единые конвенции вёрстки: заголовки страниц, карточки, строки-метрики, иконки Bootstrap.
- Облегчённая типографика (было «слишком жирно»): заголовки и подписи 700->600.
- Пересобранный «Контингент»: KPI крупными читаемыми карточками в 2 ряда + спокойная таблица.
- Выравнивание поиска на главной, доступность (aria-label/role), починен баг /olympiads/ (500).
- Палитра НЕ менялась — оставлен ваш оранжевый бренд.

КАК ЗАПУСТИТЬ ЛОКАЛЬНО (быстрый просмотр на SQLite)
  python run.py            # поднимется на http://127.0.0.1:5001
  Логин:  admin / admin123 (тестовая база instance/app.db уже внутри архива)
Тестовые пользователи: test_class_teacher / test_psychologist / test_social / test_teacher (пароль test123).

ОСТАЛОСЬ В BACKLOG (не делали — на ваше усмотрение)
- Доступность, глубокий уровень: <select> без for/id, <th> без scope, aria для модалок,
  клавиатура для статус-пикера.
- Рефакторинг наслоённого каскада app.css (рискованно, отложено осознанно).

Вопросы по конкретным экранам — пишите, поправим точечно.
