# Безопасность, GitHub и серверный релиз — шаг 12

Дата выполнения серверной части: 24 сентября 2026 года.

## Что выполнено локально

- Добавлен GitHub Actions workflow с PostgreSQL 16, полной проверкой тестов и цепочки Alembic.
- Добавлена автоматическая проверка репозитория на отслеживаемые окружения, ключи, служебные файлы и очевидные реквизиты.
- Из локальной истории Git удалено старое виртуальное окружение и прежние версии двух служебных скриптов с опубликованными реквизитами.
- Создан и проверен полный Git bundle до очистки истории: `backups/pre-step12-history-20260924.bundle`.
- Текущие версии служебных скриптов восстановлены только с чтением реквизитов из окружения.

## Что выполнено на сервере

- Подготовлен отдельный Git-релиз коммита `7afbdb98d47b1461599bf48b422733fee7695980`.
- Зависимости установлены в отдельное виртуальное окружение и проверены командой `pip check`.
- Рабочий каталог `/home/administrator/school-tracker-pwa` переведён на Git-копию; загружаемые файлы и окружение оставлены внешними для релиза.
- Создана резервная копия в `/home/administrator/altair-backups/step12-20260924-133956`:
  - архив кода и настроек до обновления;
  - архив пользовательских загрузок;
  - проверенный дамп всех доступных таблиц PostgreSQL;
  - прежнее виртуальное окружение для быстрого отката.
- Полный дамп не включил старую таблицу `email_settings`: у рабочего пользователя базы нет права чтения этой таблицы. Остальной дамп проверен через `pg_restore --list`.
- Фактическая схема сопоставлена с 203 таблицами моделей: отсутствующих таблиц и столбцов нет.
- Добавлены три отсутствовавших индекса результатов олимпиад.
- История Alembic синхронизирована с фактической схемой и зафиксирована на `c1d4e8f2a607 (head)`.
- Автоматическое создание схемы при запуске отключено: `AUTO_SCHEMA_BOOTSTRAP=0`.
- Сменены ключ сессий и опубликованный пароль PostgreSQL; настройки синхронно обновлены для обеих серверных служб.
- Gunicorn перезапущен мягко. Проверены `/healthz`, `/login`, `/lesson-visits/`, миграции, процессы и журналы ошибок.
- Публичные адреса `altair-school.ru` и `altair-edu.ru` ведут на страницу входа модуля посещений.

## Что осталось завершить в GitHub

Текущий GitHub-токен не имеет права `workflow`, поэтому GitHub отклонил публикацию `.github/workflows/ci.yml`. Кроме того, принудительная замена истории общих веток требует отдельного явного подтверждения, поскольку меняет хеши существующих коммитов.

До получения этих двух подтверждений:

- очищенная локальная история сохранена;
- старые удалённые ветки не удалялись и не перезаписывались;
- сервер работает из уже опубликованного и проверенного коммита шага 11;
- локальные изменения шага 12 готовы к публикации.

## Проверки после релиза

- PostgreSQL: подключение после смены пароля успешно.
- Alembic: `c1d4e8f2a607 (head)`.
- PWA-служба: `active`.
- Старая совместимая служба: `active`.
- Внутренний health check: HTTP 200.
- Публичный маршрут посещений: после перенаправления на вход HTTP 200.

